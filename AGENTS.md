# Repository workflow

- Every new algorithm, diagnostic, or substantial experiment implementation
  must add or update a dated Markdown note under `docs/` in the same commit.
- The note must record the motivation, mathematical objective when applicable,
  exact code/config changes, launch command, logged metrics, validation, and
  known limitations.
- Do not push implementation-only commits without the corresponding docs note.
